# Модуль 5. Расширение операций — чек-лист готовности

## Поведенческие маркеры

- [ ] Все 6 операций работают: `+`, `-`, `*`, `/`, `%`, `^`
- [ ] Каждая арифметическая операция — отдельная функция (`add`, `subtract`, ...)
- [ ] `divide` и `modulo` возвращают `(float64, error)` и проверяют деление на ноль
- [ ] `add`, `subtract`, `multiply`, `power` возвращают просто `float64` (ошибаться нечему)
- [ ] Есть функция `calculate(a, b, op) (float64, error)` — выбирает операцию
- [ ] Есть функция `parseArgs(args) (float64, string, float64, error)` — разбирает аргументы
- [ ] `main` короткий: parseArgs → calculate → вывод → обработка ошибок
- [ ] Нет глобальных переменных
- [ ] Все случаи из модуля 4 продолжают работать
- [ ] Код закоммичен в git с осмысленным сообщением

## Концептуальные маркеры

- [ ] Могу объяснить, что такое **функция как имя для логики** (а не просто способ переиспользовать код)
- [ ] Понимаю **принцип единственной ответственности** и могу применить его к своему коду
- [ ] Знаю, **когда** функция должна возвращать `error`, а когда — нет
- [ ] Понимаю, что такое **чистая функция** и почему `parseArgs` принимает `args` параметром, а не читает `os.Args` сам
- [ ] Могу объяснить, почему `main` стал коротким — это **следствие**, а не цель
- [ ] Понимаю, что `math.Mod` и `math.Pow` — это про работу со `float64`, а оператор `%` — только для целых

## Маркеры экспериментов

- [ ] Посчитал длину `main` до и после рефакторинга, увидел разницу
- [ ] Прочитал `main` вслух — звучит как описание алгоритма
- [ ] Сломал одну функцию и убедился, что остальные работают (изоляция)
- [ ] Добавил шестую операцию (например, `min`) — увидел, как мало мест надо тронуть

## Самопроверка

Закрой все файлы. Открой пустой `main.go`. Без подглядывания напиши **скелет** калькулятора:

- объявления функций с правильными сигнатурами (без тел)
- `main`, который их вызывает

Если справился — у тебя в голове есть устойчивая структура, и ты готов к проекту 2.

---

## 🎉 Поздравляю!

Ты прошёл первый полноценный проект на Go. На входе — «никогда не программировал». На выходе:

- Программа на ~150 строк кода, разложенная на функции
- Понимание базового синтаксиса Go
- Идиоматичная обработка ошибок (главный навык Go-разработчика)
- Опыт работы с командной строкой, аргументами, stdin/stdout/stderr
- Первое прикосновение к декомпозиции и принципу единственной ответственности
- Готовый репозиторий на GitHub — первый кирпич твоего портфолио

**Следующий проект — To-Do лист в памяти.** Там ты впервые встретишь слайсы и структуры — и поймёшь, что Go умеет сильно больше, чем складывать числа.

Перед тем как идти дальше — отдохни день. Серьёзно. Усваивание навыков происходит **во время отдыха**, а не во время работы. Это нейробиология, не лозунг.
