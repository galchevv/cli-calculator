# Модуль 2. Ввод от пользователя — чек-лист готовности

## Поведенческие маркеры

- [ ] Программа спрашивает у пользователя два числа и операцию
- [ ] Перед каждым вводом выводится понятное приглашение
- [ ] Поддерживаются все 4 операции: `+`, `-`, `*`, `/`
- [ ] Числа объявлены как `float64`
- [ ] Деление работает корректно (10 / 4 = 2.5, а не 2)
- [ ] Все `Scan` вызовы используют `&` перед переменными
- [ ] Код закоммичен в git

## Концептуальные маркеры

- [ ] Могу объяснить, что такое **stdin** и **stdout**
- [ ] Понимаю, зачем перед переменной в `Scan` нужен `&`
- [ ] Могу объяснить разницу между `var x int` и `x := 10` — когда какой использовать
- [ ] Понимаю, почему `Scan` блокирует выполнение программы
- [ ] Знаю разницу между `'+'` (rune) и `"+"` (string)

## Маркеры экспериментов

- [ ] Проверил, что произойдёт, если ввести букву вместо числа
- [ ] Проверил, что произойдёт при делении на ноль (для `float64`)
- [ ] Попробовал убрать `&` и прочитал сообщение компилятора
- [ ] Запомнил эти ситуации как «дыры», которые будем чинить в модуле 4

## Самопроверка

Не подсматривая в свой код, попробуй ответить:

1. Какие три типа переменных тебе понадобились? (число, число, строка)
2. Сколько раз ты вызвал `Scan`?
3. Какой оператор сравнения ты использовал для строки операции?

Если на все три вопроса ответил уверенно — переходи к модулю 3.