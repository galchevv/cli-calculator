# Модуль 1. Захардкоженный калькулятор — чек-лист готовности

> Не переходи к модулю 2, пока не отметил все пункты. Если на каком-то споткнулся — вернись к теории или к практике.

## Поведенческие маркеры (что делает код)

- [ ] Программа компилируется без ошибок и предупреждений
- [ ] При запуске `go run main.go` выводит все 4 (или 5, с остатком) операции
- [ ] Числа `20` и `4` хранятся в переменных, а не подставлены прямо в `Println`
- [ ] Результаты операций тоже сохранены в переменных
- [ ] Использовано короткое объявление `:=`
- [ ] Файл `go.mod` создан в корне проекта
- [ ] Код закоммичен в git и запушен на GitHub

## Концептуальные маркеры (что понимаю)

- [ ] Могу объяснить своими словами, что такое **переменная**
- [ ] Могу объяснить, что такое **тип переменной** и зачем он нужен в Go
- [ ] Понимаю разницу между `int` и `float64`
- [ ] Знаю, что выведет `7 / 2` и почему это **не** `3.5`
- [ ] Знаю, что выведет `7.0 / 2.0` и почему это уже `3.5`
- [ ] Понимаю, что такое `package main` и `func main`
- [ ] Понимаю, зачем нужна строка `import "fmt"`

## Маркеры эксперимента

- [ ] Поменял числа на `20` и `3`, увидел результат целочисленного деления
- [ ] Поменял типы на `float64`, увидел разницу
- [ ] Добавил пятую операцию (остаток `%`) — для целых чисел
- [ ] Могу объяснить Claude **своими словами** разницу между `int / int` и `float64 / float64`

## Самопроверка перед переходом

Закрой все файлы. Открой пустой `main.go`. Без подглядывания напиши программу, которая складывает два числа и выводит результат.

Если получилось без ошибок с первой попытки — модуль закрыт, иди дальше.
Если запутался — вернись к теории, перечитай раздел про переменные, попробуй ещё раз.

**Это не экзамен. Это проверка для тебя самого, чтобы не строить дом на песке.**